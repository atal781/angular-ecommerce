import { CheckoutFormValidations } from './checkout-form-validations';

describe('CheckoutFormValidations', () => {
  it('should create an instance', () => {
    expect(new CheckoutFormValidations()).toBeTruthy();
  });
});
